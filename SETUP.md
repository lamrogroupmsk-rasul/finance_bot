# Финансовый бот — настройка

## Что умеет бот

- Пишешь в Telegram → бот записывает в таблицу
- Автоматически определяет текущий месяц (лист)
- Каждый понедельник в 9:00 — отчёт по расходам за неделю
- /report — отчёт вручную
- /balance — баланс за месяц

---

## Шаг 1 — Создать Telegram-бота

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Напиши `/newbot`
3. Придумай имя и username (например `my_finance_bot`)
4. Скопируй токен вида `1234567890:AAHxxx...`

---

## Шаг 2 — Google Service Account (доступ к таблице)

### 2.1 Создать проект и включить API

1. Открой [console.cloud.google.com](https://console.cloud.google.com)
2. Создай новый проект (или выбери существующий)
3. В поиске найди **Google Sheets API** → нажми **Включить**
4. В поиске найди **Google Drive API** → нажми **Включить**

### 2.2 Создать сервисный аккаунт

1. В левом меню: **IAM и администрирование → Сервисные аккаунты**
2. Нажми **Создать сервисный аккаунт**
3. Дай любое имя, например `finance-bot`
4. Роль выбирать не нужно → нажми **Готово**

### 2.3 Скачать JSON-ключ

1. Кликни на созданный аккаунт
2. Вкладка **Ключи** → **Добавить ключ** → **Создать новый ключ**
3. Выбери **JSON** → скачается файл
4. Переименуй файл в `credentials.json`
5. Положи его в папку `finance_bot/`

### 2.4 Дать боту доступ к таблице

1. Открой скачанный `credentials.json`
2. Найди поле `"client_email"` — скопируй email (вида `finance-bot@project.iam.gserviceaccount.com`)
3. Открой [твою таблицу](https://docs.google.com/spreadsheets/d/1-Xj_JrWZpQuZRs57b2uSbqPUD_17xHTRgHvgJhTWxnc)
4. Нажми **Настройки доступа** → вставь этот email → роль **Редактор**

---

## Шаг 3 — Настроить .env

```bash
cd finance_bot
cp .env.example .env
```

Открой `.env` и заполни:

```
TELEGRAM_BOT_TOKEN=токен_от_botfather
ALLOWED_USER_IDS=твой_telegram_id
```

Свой Telegram ID узнай через [@userinfobot](https://t.me/userinfobot).

---

## Шаг 4 — Установить зависимости и запустить

```bash
cd finance_bot
python3 -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate
pip install -r requirements.txt
python bot.py
```

---

## Как писать боту

| Что напиши | Что запишется |
|---|---|
| `расход 1500 кафе обед` | Расход 1500₽, категория Кафе, комментарий "обед" |
| `р 500 кофе` | Расход 500₽, категория Кофе |
| `трата 3000 бензин` | Расход 3000₽, Бензин |
| `приход 50000 СК Небо зарплата` | Доход 50000₽, СК Небо |
| `п 10000 Исаев Групп` | Доход 10000₽, Исаев Групп |

Если категория не распознана — бот покажет кнопки для выбора.

### Категории расходов
Кафе · Кофе · Магазин · Медицина · Бензин · Продукты · Работа · Прочие · Домашние платежи · Вика · Расул · Кредит · Развлечения

### Категории доходов
СК Небо · Исаев Групп · Скрапмет

---

## Автозапуск (macOS)

Чтобы бот работал постоянно, можно запустить через `screen`:

```bash
screen -S finance_bot
source venv/bin/activate
python bot.py
# Ctrl+A затем D — отсоединиться (бот продолжит работу)
```

Или через `nohup`:
```bash
nohup python bot.py > bot.log 2>&1 &
```

---

## Проверка листов в таблице

Убедись что названия листов совпадают с конфигом в `config.py`:

```python
MONTH_SHEETS = {
    1: "Янв26",   2: "Февр26",  3: "Март26",
    4: "Апр26",   5: "Май26",   6: "Июнь26",
    7: "Июль26",  8: "Авг26",   9: "Сент26",
    10: "Окт26",  11: "Нояб26", 12: "Дек26",
}
```

Если название листа отличается — исправь в `config.py`.
